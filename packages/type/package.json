{
  "name": "@trz/type",
  "description": "A tool for real type validate.",
  "version": "0.3.2",
  "license": "MIT",
  "author": "CHEN·ZHENYUA <JAYNE@CHENZHENYUAN.COM>",
  "main": "lib/type.js",
  "scripts": {
    "build": "npm run clean && babel src --out-dir lib --extensions .ts,.tsx --delete-dir-on-start --no-comments --config-file='../../.babelrc' && tsc ",
    "clean": "rimraf lib",
    "obsolete::build": "npm run clean && babel src --out-dir=lib --source-maps --extensions=.ts,.tsx",
    "release": "npm run build && npm publish --access=public --registry=https://registry.npmjs.org",
    "test": "npm run build && jest"
  },

  "bugs": {
    "url": "https://github.com/chenzhenyuan/trz/issues"
  },

  "directories": {
    "lib": "lib",
    "test": "__tests__"
  },

  "files": [
    "lib/*"
  ],

  "homepage": "https://github.com/chenzhenyuan/trz/tree/release/packages/type#readme",

  "keywords": [
    "type",
    "isType",
    "typeof",
    "类型",
    "类型检验"
  ],

  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  },

  "repository": {
    "type": "git",
    "url": "git+https://github.com/chenzhenyuan/trz.git"
  },

  "devDependencies": {
    "jest": "^27.4.7"
  }
}
